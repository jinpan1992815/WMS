<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@style/Theme.PageIndicatorDefaults">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MainRadioButton">
        <item name="android:textColor">@color/radiobutton_selector</item>
        <item name="android:textSize">9sp</item>
        <item name="android:button">@null</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_width">@dimen/x140</item>
        <item name="android:layout_height">@dimen/y140</item>
    </style>
</resources>
